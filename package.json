{
  "name": "ats-analyzer",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "start": "node backend/server.js"
  }
}
